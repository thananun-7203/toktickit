# Lab 1 — AI Use and Reflection  (fill this in)

**LLM/agent used:** opencode (deepseek-v4-flash-free) and Google Gemini pro

## Selected key prompts (6–10)
| # | Prompt (summarised) | What I did with the result |
|---|---------------------|----------------------------|
| 1 | ช่วยอ่านและทำความเข้าใจไฟล์แลปทั้ง 3 ไฟล์ ที่เป็นไฟล์ .pdf อย่างละเอียด และช่วยทำ Lab plan ที่เป็นไฟล์ .md ให้ด้วย | เช็คความถูกต้องของไฟล์ .md และนำไปใช้เป็น Checklist ทีละขั้นตอนในการทำแลปจริง |
| 2 | ช่วยบอกแผนการทำ Issue 4 — Display the IT request category list  โดยอิงตามเกณฑ์ Acceptance criteria และบอกด้วยว่าต้องใส่ข้อมูลอะไรไนโฟลเดอร์ doc มั้ย ตอบคำถามฉันพอยังไม่ต้องทำอะไร | เช็คขั้นตอนการพัฒนากับเกณฑ์ที่กำหนด ว่าแผนที่วางมานั้นถูกต้องตาม Acceptance criteria ของ Issue 4 หรือยัง |
| 3 | ช่วยตรวจสอบให้ทีว่าฉันทำ issue 3 เสร็จเรียบร้อยครบทุกส่วนยัง และมีส่วนไหนต้องแก้ไขบ้าง | ตรวจสอบความถูกต้องก่อนส่งให้เพื่อน PR ต่อไป|
| 4 | มีวิธีให้ฉันตรวจสอบเองมั้ยว่าทำ issue 3 ผ่าน ตอบคำถามฉันพอยังไม่ต้องทำอะไร | นำวิธีการตรวจสอบที่แนะนำไปทดสอบระบบด้วยตัวเอง เพื่อให้แน่ใจว่าผ่าน Acceptance Criteria ของ Issue 3 ครบทุกข้อ|
| 5 | ช่วยตรวจสอบไฟล์ tests.md ให้ทีว่าถูกต้องตาม CHANGES_REQUESTED ของเพื่อนหรือยัง โดยอย่าพึ่งอัพไฟล์ขึ้น git นะ และแค่ตรวจสอบพอยังไม่ต้องทำอะไร | นำผลการตรวจสอบไปเช็คว่าไฟล์ tests.md ถูกแก้ไขตาม CHANGES_REQUESTED เรียบร้อยแล้ว ก่อนที่จะอัปโหลดขึ้น Git |
| 6 | ช่วนเขียนคำตอบที่คุณพึ่งตอบมาเป็น prompt เพื่อให้ฉันเอาไปให้เอไอตัวอื่นพารันและแคปภาพหลักฐานที | นำ prompt ที่ได้ไปใช้กับ Ai อีกตัวเพื่อช่วยในการพาถ่ายรูปเก็บหลักฐาน |
| 7 | ฉันแก้ไขไฟล์ teste.md ผิดพลาด ช่วยคืนค่าไฟล์ tests.md ก่อนจะมีการแก้ไขที่ผ่านมาทั้ง 2 ครั้งได้มั้ย | ได้ไฟล์ tests.md ฉบับที่แก้ไขแล้วกลับคืนมา |
| 8 | ถ้าฉันส่งลิงก์ PR ของเพื่อนมา เช่น https://github.com/<เพื่อน>/toktickit/pull/...  คุณสามารถเข้าไปดูและ review เป็นแนวทางให้ได้มั้ย โดยห้ามแก้ไขอะไรของเพื่อนเด็ดขาด แค่ดูและตรวจพร้อมส่งคอมเม้นให้ฉันก่อน โดยฉันจะเป็นคนทำการเลือกเองว่าจะเอาคอมเม้นนี้มั้ย ตอบคำถามฉันพอยังไม่ต้องทำอะไร | นำ comment ที่ได้มาดูและเช็คด้วยตัวเองอีกรอบ และเรียบเรียงใหม่ให้เหมาะสมและถูกต้อง|
| 9 | อยากให้เริ่มตรวจ lab1-staging ว่าพร้อม release ไหม แค่ตรวจก่อนนะอย่าพึ่งทำอะไร | นำผลการตรวจสอบมาพิจารณาการ release |
| 10 | ถ้าแก้ไขไฟล์ client/src/api.ts และ server/tests/lab-01/categories.test.ts จะส่งผลอะไรมั้ย และต้องแคปหลักฐานใหม่หรือเปล่า ตอบคำถามฉันพอยังไม่ต้องทำอะไร | นำคำตอบที่ได้มาดูว่าส่ผลเสียต่อระบบไหม และทำการแคปหลักฐานใหม่เนื่องจากมีการแก้ไขไฟล์แสดงผล |

## Reflection
Two or three sentences: what made your prompts better, and one place you had to
correct or reject what the agent produced. : 
- จากการได้ใช้ Ai prompt มา ทำให้ได้รู้ว่า การที่เราจะต้องการคำตอบที่ดีนั้น ขึ้นอยู่กับว่าเราจะ prompt Ai ตัวนั้นให้เข้าใจปัญหาที่เราจะสื่อแค่ไหน ดังนั้นหากเราต้องการให้ Ai พัฒนาระบบให้เรา เราควรบอกรายละเอียดของระบบให้Ai เข้าใจอย่างละเอียดก่อน
- เรานั้นยังไม่ควรเชื่อคำตอบของ Ai ในทันที แต่ควรหาวิธีที่จะช่วยให้เราสามารถตรวจสอบคำตอบของ Ai ได้ ซึ่งในกรณีคือผมได้ทำการตรวบสอบการเทสด้วยตัวเองทุกครั้งหลักจากที่ Ai เทสมาให้ผมแล้ว
- สิ่งที่ผมควรแก้ไขคือ การที่ใช้ Ai ในการถามจี้จุกจิกเกินไปทำให้เปลือง Token

